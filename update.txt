# 🚨 Recuperar Menu Original (React)

O agente anterior destruiu meu menu, reduziu para 3 opções e excluiu o arquivo original.  
Sua tarefa é **recuperar o menu completo que havia antes**, exatamente como estava.

## ✅ Passos obrigatórios

1. **Localizar commit da exclusão/modificação do menu**
   - Rode: `git log --diff-filter=D --summary | grep "Menu"`
   - Se não achar, busque pelo conteúdo:
     - `git log -S "export default function Menu" -p -- src`
     - ou `git log -S "<nav"` -p -- src`

2. **Recuperar o arquivo**
   - Use: `git checkout <commit_id>^ -- src/components/Menu.tsx`
   - Se o nome era outro (Sidebar, Navigation, etc.), recupere pelo conteúdo.

3. **Recolocar no projeto**
   - Coloque o arquivo restaurado em: `@/components/Menu`
   - Atualize o `AppLayout` para importar e usar esse menu.

4. **Garantir integridade**
   - Verifique se todas as rotas existentes aparecem no menu.
   - Ajuste imports para usar **aliases** (`@/pages/...`, `@/components/...`).

5. **Documentar em `REFATORACAO.md`**
   - Commit de onde recuperou o menu.
   - Nome do arquivo restaurado.
   - Ajustes feitos (se houver).
   - Status de validação (funcional, compilando, rotas certas).

## ❌ Não permitido
- Não inventar um menu novo.
- Não simplificar para menos opções.
- Não ignorar documentação no `REFATORACAO.md`.

---

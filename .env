# Environment variables for Docker Compose setup

# Backend Server Configuration
PORT=5000
NODE_ENV=development

# PostgreSQL Database Configuration
# These values will be used by the 'postgres' service and the 'backend' service
DB_HOST=postgres # Changed from localhost to 'postgres' for Docker Compose
DB_PORT=5432
DB_NAME=feedback_restaurante
DB_USER=postgres
DB_PASSWORD=123456789

# JWT Secret for Backend Authentication
JWT_SECRET=YrvWABCrjcTJ73ScR3Z1IQUs6Y0EnnOC
JWT_EXPIRES_IN=7d # Using EXPIRES_IN for consistency with docker-compose.yml

# URLs for inter-service communication or external access
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:5000

# Frontend API URL
# This is used by the React frontend to know where to send API requests
REACT_APP_API_URL=http://localhost:5000/api